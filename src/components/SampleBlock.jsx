

function SampleBlock({title, description}) {

    return (
        <div className="bg-green-200"> 
        <h2 className="text-lg">{title}</h2>
        <h2 className="text-sm">{description}</h2>
        </div>
    )
}

export default SampleBlock